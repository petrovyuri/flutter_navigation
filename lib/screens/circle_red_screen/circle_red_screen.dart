import 'package:flutter/material.dart';

class CircleRedScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Circle Red Screen"),
      ),
    );
  }
}